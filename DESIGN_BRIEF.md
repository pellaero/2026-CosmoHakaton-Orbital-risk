# Бриф: интерфейс «Анализ рисков ВКД на МКС»

## 1. Что это за продукт

Веб-приложение для поддержки решений при планировании выхода в открытый космос (ВКД) с МКС.
Исследовательский прототип для КосмоХакатона 2026.

Пользователь выбирает дату и одно или два временных окна для выхода. Система оценивает риск каждого окна
по 4 механизмам воздействия и рекомендует лучшее окно. Главная ценность — не просто цифра риска,
а **доказательная база**: из каких факторов он сложился, какие пороги превышены, откуда взяты данные.

Ключевой тезис для жюри: простой подход («Kp ≥ 5 — отмена») сравнивается с многофакторным алгоритмом команды.
Интерфейс должен наглядно показывать эту разницу.

## 2. Пользователи

- **Планировщик ВКД / специалист ЦУПа** — принимает решение «выходить / не выходить / перенести».
  Ему нужно за несколько секунд увидеть вердикт, а потом при необходимости раскрыть детали.
- **Жюри хакатона** — оценивает обоснованность алгоритма и то, как он превосходит простой подход.

Язык интерфейса — русский. Время везде в UTC, и это должно быть явно подписано.

## 3. Входные параметры (все нужны в дизайне)

| Параметр | Тип | Значения |
|---|---|---|
| Режим работы | переключатель | «Исторический режим (Replay)» / «Текущая обстановка» |
| Дата | поле даты | YYYY-MM-DD, по умолчанию 2024-05-11 (день экстремальной геомагнитной бури — демо-кейс) |
| Быстрый выбор даты | кнопки | «Сегодня», «Завтра» |
| Режим прогноза на 3 дня | переключатель / отдельный сценарий | берёт текущую дату, перебирает окна и ищет лучшее |
| Окно 1 — начало | время ЧЧ:ММ UTC | по умолчанию 12:00 |
| Окно 2 — начало | время ЧЧ:ММ UTC | по умолчанию 18:00 |
| Длительность ВКД | слайдер | 1–8 часов, шаг 1, по умолчанию 6 |
| Принудительно обновить кэш | чекбокс | для опытных пользователей, можно убрать во «Дополнительно» |
| Запустить анализ | главная кнопка | |

Проблема текущего UI: кнопка «Прогноз на 3 дня», чекбокс «Режим прогноза» и переключатель режима дублируют друг друга
и путают. Нужно свести это к двум понятным сценариям: **«Сравнить два окна»** и **«Найти лучшее окно (прогноз)»**.
Окна удобнее показывать на временной шкале суток, а не четырьмя числовыми полями «час/минута».

## 4. Что выдаёт расчёт

### 4.1. Для каждого окна

- **Интегральный риск** 0–100 (алгоритм команды).
- **Базовый риск** 0–100 (простой подход: только Kp; бывает 0, 100 или 50, если данных нет).
- **Освещённость:** минуты на свету, минуты в тени, доля тени в %.
- **Блики:** минимальный угол между Солнцем и Землёй в градусах и сколько минут он был < 5°.
- **MMOD (космический мусор):** число опасных сближений < 5 км или «данные недоступны».
- **Критические флаги** (красные) и **предупреждения** (жёлтые) — короткие строки, например «Протоны 45.2 pfu (>10.0)», «Kp 8.7 (Экстрем. буря)».

### 4.2. Доказательная база (карточки факторов)

Каждый сработавший фактор содержит:
- название («Протоны >10 МэВ», «Kp-индекс», «MMOD», «Доля тени», «Солнечные блики»);
- механизм, к которому он относится;
- фактическое значение и порог;
- статус: `CRITICAL` / `WARNING` / `UNKNOWN`;
- пояснение простым языком, например «Превышен порог радиационной безопасности. ВКД отменяется.»

### 4.3. Итог

- **Сравнение двух окон:** «Окно 1 / Окно 2 имеет меньший риск» или «Окна равнозначны».
- **Прогноз:** лучшее окно (дата и время) и его риск, а также вывод вида «Алгоритм команды нашёл безопасное окно» / «Обстановка спокойная» / «Ситуация критическая».
- **Сравнение с простым подходом:** риск команды и базовый риск рядом. Это ключевой элемент для жюри, он должен быть заметным.

### 4.4. График

Kp-индекс во времени (линия) с пунктирными порогами Kp = 5 (оранжевый) и Kp = 7 (красный).
Анализируемые окна показаны закрашенными полосами цвета их уровня риска.
Хорошо бы дополнительно показать на той же оси поток протонов и участки тени и света, но это необязательно.

### 4.5. Экспорт

Кнопка «Скачать отчёт (JSON)».

## 5. Шкала риска и цвета

| Уровень | Диапазон | Цвет |
|---|---|---|
| Низкий | < 40 | зелёный |
| Средний | 40–69 | оранжевый |
| Высокий | ≥ 70 | красный |

Статус `UNKNOWN` (данные недоступны) — отдельный нейтральный цвет (серый). Это **не то же самое**, что «безопасно».
Цвет не должен быть единственным носителем смысла: дублировать иконкой и текстом (доступность, дальтонизм).

Как складывается риск (стоит показать как разложение, например столбиком или списком слагаемых):
протоны > 10 pfu → +40; Kp ≥ 7 → +30 (или Kp ≥ 5 → +15); MMOD-сближения → +20; тень > 50 % → +10; блики → +10; максимум 100.

## 6. Четыре механизма воздействия

1. **Космическая погода** — Kp-индекс (геомагнитная буря) и поток протонов > 10 МэВ (радиация).
2. **Орбитальная обстановка** — сближения с космическим мусором (MMOD).
3. **Освещённость** — доля времени в тени Земли (риск переохлаждения скафандра).
4. **Солнечные блики** — угол Солнца близко к направлению на Землю (ослепление визора).

Механизмы стоит визуально сгруппировать, чтобы было видно, что алгоритм «смотрит» на все четыре, а не только на Kp.

## 7. Источники данных и достоверность (важно)

Данные приходят из внешних сервисов: CelesTrak (орбита МКС), NOAA SWPC (Kp-прогноз, протоны),
GFZ Potsdam (архив Kp), CelesTrak SOCRATES (сближения). Если сервис недоступен, приложение
переходит на **встроенный снапшот** данных за 11.05.2024.

В дизайне нужно:
- показывать для каждого показателя **источник и его тип**: «Наблюдение (Факт)», «Внешний прогноз», «Локальный кэш»;
- **заметно** помечать, когда используются снапшот или кэш, а не живые данные. Пользователь не должен принять старые данные за текущие;
- показывать статус кэша: какие данные и как давно загружены («Kp: 3 мин назад»).

## 8. Состояния интерфейса

- **Пустое** — до первого запуска: подсказка, что сделать, и ссылка на демо-кейс 11.05.2024.
- **Загрузка** — расчёт занимает от нескольких секунд до десятков секунд (в режиме прогноза перебираются 49 окон). Нужен прогресс или скелетон.
- **Результат: всё спокойно** (риск 0, флагов нет).
- **Результат: критично** (демо 11.05.2024: окно 12:00 — 70/100, протоны 45.2 pfu и Kp 8.7; окно 18:00 — 30/100, Kp 7.7).
- **Частично нет данных** — например, MMOD недоступен (`UNKNOWN`).
- **Ошибка** — не удалось загрузить прогноз, неверная дата.

## 9. Экраны для дизайна

1. Главный экран — сравнение двух окон (параметры + результат).
2. Главный экран — режим «Найти лучшее окно / прогноз на 3 дня».
3. Детали окна — развёрнутая доказательная база и разложение риска.
4. Состояния из п. 8 (хотя бы загрузка, критично, нет данных, ошибка).

## 10. Визуальное направление и ограничения

- Характер: инструмент центра управления полётами — серьёзный, плотный по информации, без игривости. Тёмная тема как основная уместна, светлая тоже нужна.
- Иерархия: **вердикт → сравнение с простым подходом → факторы → график → источники данных**.
- Цифры моноширинным или табличным шрифтом, чтобы колонки выравнивались.
- Основная ширина — десктоп 1440 px, адаптация до планшета (768 px).
- Сейчас приложение сделано на Gradio (Python). Дизайн не обязан под него подстраиваться, но хорошо бы, чтобы основные блоки (форма слева, результаты справа, график Plotly) можно было реализовать без большой переделки.
